# Install

```pnpm
pnpm add grammy

pnpm add -D typescript
pnpm exec tsc --init
pnpm install -D @types/node
pnpm add dotenv
pnpm add -D @types/node
pnpm add -D tsx nodemon
pnpm add winston
pnpm approve-builds
pnpm add @grammyjs/hydrate
```
